services:
  # web:
  #   image: nginx
  #   volumes:
  #     - ./nginx/nginx.conf:/tmp/nginx.conf
  #   environment: 
  #     - FAST_SERVER_ADDR=backend:9091  
  #   command: /bin/bash -c "envsubst < /tmp/nginx.conf > /etc/nginx/conf.d/default.conf && nginx -g 'daemon off;'" 
  #   ports:
  #     - 80:80
  #   depends_on:
  #     - api

  api:
    build:
      context: .
      target: builder
    container_name: fastapi-application
    ports:
      - 80:80
    restart: "no"
    depends_on:
      -  mongo  

  mongo:
    image: mongo
    ports: 
      - 27017:27017
      